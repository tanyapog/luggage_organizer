import 'package:flutter/material.dart';

class AppPadding {
  const AppPadding._();

  static const vertical15 = EdgeInsets.symmetric(vertical: 15);
  static const horizontal = EdgeInsets.symmetric(horizontal: 25);
}